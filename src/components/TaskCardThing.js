import React from "react";
import PropTypes from "prop-types";

const TaskCardThing = ({ data }) => {
  console.log(`DATA FROM TASKCARD THING${data}`);
  return <p>POOP</p>;
};

export default TaskCardThing;
